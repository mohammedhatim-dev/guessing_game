# Number Guessing Game
A simple interactive game built using Python where the user tries to guess a randomly generated number.

## Features
* Uses Python's `random` module to generate numbers.
* Provides feedback to the user (Too high, Too low) until they guess right.
* Tracks the number of attempts taken by the player.
